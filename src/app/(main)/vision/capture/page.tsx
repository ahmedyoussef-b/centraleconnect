// Placeholder for src/app/(main)/vision/capture/page.tsx
'use client';

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function CapturePage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Capture Photo/Vidéo</CardTitle>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Cette page permettra la capture de photos et de vidéos.</p>
            </CardContent>
        </Card>
    );
}
